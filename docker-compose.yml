version: '3.1'

services:
    cassandra:
        image: cassandra:latest
    smacktest:
        build:
            context: .
            args:
                PATH: yelp_dataset.tar.gz
        volumes:
            - ./data:/data/parse
        depends_on: 
            - cassandra
